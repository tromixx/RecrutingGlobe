<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recruiterglobe">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Chat.ChatActivity"></activity>
        <activity android:name=".AthleteSetProfileActivity" />
        <activity android:name=".CoachSetProfileActivity" />
        <activity android:name=".AtheleteRegistrationActivity" />
        <activity android:name=".AtheleteLoginActivity" />
        <activity android:name=".CoachLoginActivity" />
        <activity android:name=".CoachRegistrationActivity" />
        <activity android:name=".AthleteProfileActivity" />
        <activity android:name=".login_option_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>